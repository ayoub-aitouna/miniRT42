#include "headers/matrex.h"
#include "../headers/types.h"

matrex_t *Set_transform(matrex_t *translation, matrex_t *rotation, matrex_t *scal)
{
    return (NULL);
}

matrex_t *Apply_transform(ray_t *poi, matrex_t *matrex_transform)
{

    return (NULL);
}