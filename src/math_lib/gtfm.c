#include "headers/matrix.h"
#include "../headers/types.h"

matrix_t *Set_transform(matrix_t *translation, matrix_t *rotation, matrix_t *scal)
{
    return (NULL);
}

matrix_t *Apply_transform(ray_t *poi, matrix_t *matrix_transform)
{

    return (NULL);
}